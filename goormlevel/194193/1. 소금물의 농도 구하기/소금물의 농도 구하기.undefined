#include <iostream>
#include <cmath>
using namespace std;
int main() {
	float n,m;
	cin >> n >> m;

	float salt = (n * 7) / 100;

	float sum = n + m;

	float answer = salt / sum * 100;
	answer = floor(answer * 100)/100;

	cout << fixed;
	cout.precision(2);
	cout << answer;
	return 0;
}